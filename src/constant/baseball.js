const LENGTH = 3;
const START_NUM = 1;
const END_NUM = 9;
const THREE_STRIKE = 3;

const COMMAND = Object.freeze({
  restart: 1,
  quit: 2,
});

module.exports = { COMMAND, LENGTH, START_NUM, END_NUM, THREE_STRIKE };
